import React, { useState } from "react";

// @ts-ignore
export default function BuildingUI(props) {
  return (
    <div>
      <p>{/* Insert Building UI here including name, health, and effect */}</p>
    </div>
  );
}
